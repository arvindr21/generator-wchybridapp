language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wchybridapp' ]; then cd .. && eval "mv $currentfolder generator-wchybridapp" && cd generator-wchybridapp; fi

